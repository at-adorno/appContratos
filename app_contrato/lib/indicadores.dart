import 'package:flutter/material.dart';

class TelaIndicadores extends StatelessWidget{

  @override
  Widget build (BuildContext context){

    return Container(
      child: Text('Indicadores'),
      );
  }
}